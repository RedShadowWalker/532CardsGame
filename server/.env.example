# Port the server listens on. Railway/Render set this automatically.
PORT=3001

# Comma-separated list of allowed frontend origin(s), e.g. your Vercel URL.
# Leave unset for local development (defaults to allowing any origin).
CLIENT_ORIGIN=https://your-client.vercel.app
